{
  "name": "hephaisto-visual",
  "private": true,
  "version": "0.0.0",
  "description": "Visual baselines for the Hephaisto design language, taken against design/gallery.html",
  "scripts": {
    "test": "playwright test",
    "update": "playwright test --update-snapshots"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1"
  }
}
